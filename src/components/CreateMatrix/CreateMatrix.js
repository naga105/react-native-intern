import {React} from 'react';
import {Text, View} from 'react-native';
import style from './style.module.css';
const CreateMatrix = () => {
  return (
    <View style={{flex: 1, alignItems: 'center'}}>
      <Text>Create New Approval Matrix</Text>
    </View>
  );
};
export default CreateMatrix;
